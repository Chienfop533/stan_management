import IconifyIcon from '@/core/components/icon'
import { IconButton } from '@mui/material'

const EventDropdown = () => {
  return (
    <>
      <IconButton color='inherit'>
        <IconifyIcon icon='majesticons:bookmark-line' />
      </IconButton>
    </>
  )
}

export default EventDropdown
